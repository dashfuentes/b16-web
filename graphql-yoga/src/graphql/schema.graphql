type Query {
    getNotes: [Note]
   # Find by title 
}

type Mutation {
 createNote(title:String, content: String, date: String, author: String) : Note 
 # update
 # delete
}

type Note {
    _id : ID
    title: String
    content: String
    date: String
    author: String
}